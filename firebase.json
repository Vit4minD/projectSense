{
  "firestore": [
    {
      "database": "prodsense",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    }
  ],
  "database": [
    {
      "instance": "REPLACE_WITH_REBUILD_RTDB_INSTANCE",
      "rules": "database.rules.json"
    }
  ],
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "database": { "port": 9000 },
    "ui": { "enabled": true, "port": 4000 },
    "singleProjectMode": true
  }
}
